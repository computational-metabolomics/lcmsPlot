% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/app-launcher.R
\name{lcmsPlotApp}
\alias{lcmsPlotApp}
\title{Launch the lcmsPlot interactive Shiny app}
\usage{
lcmsPlotApp(max_upload_size = 2 * 1024^3)
}
\arguments{
\item{max_upload_size}{A \code{numeric} value in bytes setting the maximum
per-file upload size. Defaults to \code{2 * 1024^3} (2 GB) since raw mzML
files routinely exceed Shiny's 5 MB default. Set with care if running
the app on shared infrastructure.}
}
\value{
A \link[shiny:shinyApp]{shiny::shinyApp} object.
}
\description{
Returns a \link[shiny:shinyApp]{shiny::shinyApp} object that lets users upload one or more raw
files (mzML / CDF) and explore them through the existing \verb{lp_*()} plotting
API: base-peak / total-ion chromatograms, extracted ion chromatograms,
peak density, and spectra by scan index. Each plot tab carries a side
Options panel for \code{lp_facets}, \code{lp_arrange}, \code{lp_legend}, and \code{lp_labels}.
}
\details{
Per Bioconductor's Shiny guidelines the function does \emph{not} call
\code{\link[shiny:runApp]{shiny::runApp()}} itself — callers run the returned app with
\code{shiny::runApp(lcmsPlotApp())} or by printing it at the R prompt.

Requires the optional packages \code{shiny} and \code{shinytoastr}; both are
declared in \code{Suggests}. An informative error is raised if either is
missing.
}
\examples{
if (interactive()) {
    shiny::runApp(lcmsPlotApp())
}
}
