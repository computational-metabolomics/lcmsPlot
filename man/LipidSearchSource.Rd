% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data-source-lipid-search.R
\name{LipidSearchSource}
\alias{LipidSearchSource}
\title{Create a data source from a LipidSearch result file}
\usage{
LipidSearchSource(
  results_path,
  sample_paths,
  metadata = NULL,
  keep_rejected = FALSE
)
}
\arguments{
\item{results_path}{A \code{character} path to a LipidSearch result file (4.2 or
5.2).}

\item{sample_paths}{A \code{character} vector of paths to the raw sample files
(e.g. \code{.raw} or \code{.mzML}) used to extract chromatograms. For 5.2 files this may
be named by sample key (see \emph{Sample-to-file mapping}).}

\item{metadata}{A \code{data.frame} (optional) of additional sample metadata. It
must contain one row per sample, aligned with \code{sample_paths}. When supplied,
its columns are column-bound onto the generated metadata.}

\item{keep_rejected}{A \code{logical} value indicating whether lipids flagged as
rejected (\code{Rej.}/\code{Rej}) should be retained. Defaults to \code{FALSE}.}
}
\value{
An object of class \code{LipidSearchSource}.
}
\description{
Reads a LipidSearch result file and builds a data source that can be plotted
with \code{lcmsPlot()}. Both \strong{LipidSearch 4.2} and \strong{5.2} exports are supported;
the version is auto-detected. Chromatograms are later extracted from the raw
files supplied via \code{sample_paths}.
}
\section{Sample-to-file mapping}{

A LipidSearch 4.2 file names the analysed raw files in its header
(\verb{#[c-1]:sample.raw}), so paths are matched to samples by file basename
without extension - a result file that lists ThermoFisher \code{.raw} files works
with converted \code{.mzML} files. A LipidSearch 5.2 file carries \strong{no} filenames;
its samples are keyed \code{s1}, \code{s2}, ... (from the \code{OrgMeanArea[...]} columns).
For 5.2, supply either a \strong{named} \code{sample_paths} vector whose names are those
keys (\code{c("s1" = "a.mzML", "s2" = "b.mzML")}, order-independent, any subset),
or an \strong{unnamed} vector matched positionally (\code{s1}, \code{s2}, ... in order).
}

\section{Samples beyond the result file}{

\code{sample_paths} - not the result file - determines which samples are plotted.
A path that matches no sample (an unnamed extra for 5.2, or an unmatched
basename for 4.2) is still a full sample: for every queried lipid its
chromatogram is extracted using the lipid's consensus m/z and retention-time
window (the median across the samples in which LipidSearch did detect it,
falling back to the reported \code{BaseRt} for 5.2), it simply has no reported
peak to highlight.
}

\examples{
\dontrun{
## LipidSearch 4.2 (raw files declared in the header, matched by basename):
ds <- LipidSearchSource(
    results_path = "LIPIDS_POS_5ppm.txt",
    sample_paths = c("path/to/sample1.raw", "path/to/extra_sample.raw")
)

## LipidSearch 5.2 (no filenames; map by sample key, extra sample unnamed):
ds <- LipidSearchSource(
    results_path = "LIPIDS_POS_5ppm_LS5.txt",
    sample_paths = c(
        "s1" = "path/to/sample1.mzML",
        "s2" = "path/to/sample2.mzML",
        "path/to/extra_sample.mzML"
    )
)

lcmsPlot(ds) +
    lp_lipid_search(
        lipids_query = 'class == "AcCa" & grade == "A"',
        rt_extend = 30
    ) +
    lp_chromatogram(highlight_peaks = TRUE) +
    lp_grid(rows = "sample_id", cols = "name", free_x = TRUE)
}
}
\seealso{
\code{\link[=lp_lipid_search]{lp_lipid_search()}}
}
