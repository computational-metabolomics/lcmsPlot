% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/creators-chromatograms.R
\name{create_compound_chromatograms}
\alias{create_compound_chromatograms}
\title{Extract one chromatogram per compound and sample from the raw files}
\usage{
create_compound_chromatograms(all_compounds, metadata, options, rt_extend)
}
\arguments{
\item{all_compounds}{A \code{tibble} with at least \code{name}, \code{formula}, \code{adduct},
\code{mz}, \code{rt}, \code{rtmin}, \code{rtmax}, \code{into}, \code{maxo}, and \code{sample_index} columns.
Optional \code{class} and \code{detected} columns are used when present.}

\item{metadata}{A \code{tibble} of sample metadata.}

\item{options}{A \code{list} of plot options.}

\item{rt_extend}{A \code{numeric} value indicating how much (in seconds) to extend
the RT window on each side of the compound peak.}
}
\value{
A named \code{list} with elements \code{chromatograms}, \code{mass_traces},
\code{feature_metadata}, and \code{detected_peaks}.
}
\description{
Shared by the compound-centric data sources (Compound Discoverer scripting
node, LipidSearch). Each row of \code{all_compounds} describes where to extract a
compound in one sample: an \code{mz}, an \code{rtmin}/\code{rtmax} window, and the
\code{sample_index} it belongs to.
}
\details{
Compounds with no \code{mz} or no RT window are skipped, as are compounds absent
from a given sample. When \code{all_compounds} carries a \code{detected} column, a
\code{detected_peaks} row - the one the \code{highlight_peaks} layer draws - is emitted
only where it is \code{TRUE}, so compounds that were looked for but not found are
still plotted without a degenerate highlight.
}
\keyword{internal}
