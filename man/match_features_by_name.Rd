% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers-data.R
\name{match_features_by_name}
\alias{match_features_by_name}
\title{Select grouped peaks by feature name}
\usage{
match_features_by_name(grouped_peaks, feature_names, warn = TRUE)
}
\arguments{
\item{grouped_peaks}{A \code{tibble} of grouped peaks as returned by
\code{\link[=get_grouped_peaks]{get_grouped_peaks()}}, or \code{NULL}.}

\item{feature_names}{A \code{character} vector of feature identifiers to keep.}

\item{warn}{A \code{logical} value indicating whether to warn about identifiers
that match no feature.}
}
\value{
A \code{tibble} with the matching grouped peaks; possibly with zero rows.
}
\description{
Subsets the grouped peaks (features) to those whose \code{name} matches one of
the supplied identifiers. Feature identifiers are the row names of the
object's feature definitions, i.e. whatever the user has set them to, or the
\code{"FT0001"}-style defaults xcms assigns during correspondence.
}
\keyword{internal}
