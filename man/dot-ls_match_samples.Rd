% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data-source-lipid-search.R
\name{.ls_match_samples}
\alias{.ls_match_samples}
\title{Match sample paths to the file's declared samples}
\usage{
.ls_match_samples(declared, sample_paths)
}
\arguments{
\item{declared}{A \code{tibble} of sample declarations from \code{.ls_read_results()}.}

\item{sample_paths}{A \code{character} vector of raw sample file paths.}
}
\value{
An \code{integer} vector of \code{nrow == length(sample_paths)} of row indices
into \code{declared}, or \code{NA} for extra samples.
}
\description{
Returns, for each \code{sample_paths} entry, the index into \code{declared} it maps to
(\code{NA} for an extra sample). LipidSearch 4.2 files name their raw files, so
paths are matched by basename without extension. LipidSearch 5.2 files carry
no filenames: a named \code{sample_paths} vector is matched by its names against
the sample keys (\code{s1}, \code{s2}, ...), and an unnamed vector is matched
positionally.
}
\keyword{internal}
