% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/app-utils.R
\name{.eic_features}
\alias{.eic_features}
\title{Build a features matrix for an extracted ion chromatogram}
\usage{
.eic_features(mz, ppm, rt = NULL, rt_tol = NULL)
}
\arguments{
\item{mz}{A \code{numeric} scalar — the target m/z.}

\item{ppm}{A \code{numeric} scalar — the ppm tolerance.}

\item{rt}{A \code{numeric} scalar or \code{NULL} — the target retention time (in
the same unit as the data, typically seconds).}

\item{rt_tol}{A \code{numeric} scalar — the symmetric RT tolerance. Ignored
when \code{rt} is \code{NULL}.}
}
\value{
A 1x4 \code{matrix} with column names \code{mzmin}, \code{mzmax}, \code{rtmin}, \code{rtmax}.
}
\description{
Produces the single-row \code{features} matrix expected by \code{\link[=lp_chromatogram]{lp_chromatogram()}}
from a target \code{mz}, a ppm tolerance, and an optional retention-time window.
When \code{rt} is \code{NULL} the RT columns are filled with \code{NA} so the full RT
range is plotted.
}
\keyword{internal}
