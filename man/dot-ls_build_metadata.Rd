% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data-source-lipid-search.R
\name{.ls_build_metadata}
\alias{.ls_build_metadata}
\title{Build the sample metadata for a LipidSearch source}
\usage{
.ls_build_metadata(declared, sample_paths, metadata = NULL)
}
\arguments{
\item{declared}{A \code{tibble} of sample declarations from \code{.ls_read_results()}.}

\item{sample_paths}{A \code{character} vector of raw sample file paths.}

\item{metadata}{A \code{data.frame} (optional) of additional sample metadata with
one row per \code{sample_paths} entry.}
}
\value{
A \code{tibble} with \code{sample_index}, \code{sample_id}, \code{sample_path},
\code{sample_key}, \code{sample_group} and \code{in_results}.
}
\description{
\code{sample_paths} is authoritative: every supplied path becomes a sample. Paths
are matched to the result file's samples by \code{.ls_match_samples()} - by
filename for 4.2, by sample key / position for 5.2. Paths that match nothing
are kept as extra samples, plotted from each lipid's consensus m/z and RT
window.
}
\keyword{internal}
