% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lcmsPlot-class.R
\name{lp_peak_count_image}
\alias{lp_peak_count_image}
\title{Plot chromatographic peak counts per retention-time bin}
\usage{
lp_peak_count_image(
  bin_size = 30,
  log = FALSE,
  sample_ids = NULL,
  rt_range = NULL,
  fill_scale = NULL
)
}
\arguments{
\item{bin_size}{A \code{numeric} value giving the retention-time bin width in
seconds. Defaults to \code{30}.}

\item{log}{A \code{logical} value. When \code{TRUE}, counts are shown on a \code{log2}
scale, which is the usual way to read the plot when counts are skewed. Empty
bins are left blank rather than being drawn as \code{-Inf}.}

\item{sample_ids}{A \code{character} vector of sample IDs to include.
\code{NULL} uses all samples.}

\item{rt_range}{An optional length-2 \code{numeric} limiting the binned
retention-time range. When \code{NULL} (default) the full acquisition range of the
data object is used, so a short run shows as empty bins at the edge.}

\item{fill_scale}{A ggplot2 scale object to use for the fill aesthetic. When
\code{NULL} (default), uses \code{scale_fill_viridis_c}.}
}
\value{
This function returns another function that takes an \code{lcmsPlot}
object and produces a modified version containing the generated peak counts
in its \code{data} slot. It is designed to be used with the \code{+} operator,
which serves as a layering mechanism.
}
\description{
\code{lp_peak_count_image()} produces the quality-control view drawn by
\code{xcms::plotChromPeakImage()}: retention-time bins on the x axis, samples on
the y axis, and fill showing how many chromatographic peaks each sample
yielded in each bin. It is how you spot a sample that stopped producing peaks
half-way through a run, or a retention-time region where detection collapsed.
}
\details{
Samples are ordered by injection order rather than alphabetically, and bins
containing no peaks are drawn as zero rather than dropped, so a failing
sample reads as an empty stretch instead of vanishing from the plot.
}
\examples{
data_obj <- get_XCMSnExp_object_example(
  indices = 1:3,
  should_group_peaks = TRUE)

p <- lcmsPlot(data_obj, sample_id_column = "sample_name") +
  lp_peak_count_image(bin_size = 30)
p
}
