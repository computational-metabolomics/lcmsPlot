% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/app-utils.R
\name{.uploaded_files_to_paths}
\alias{.uploaded_files_to_paths}
\title{Copy uploaded Shiny files to a session temp dir under their original names}
\usage{
.uploaded_files_to_paths(upload, dir)
}
\arguments{
\item{upload}{A \code{data.frame} as produced by \code{shiny::fileInput()} with at
least the columns \code{datapath} and \code{name}. May be \code{NULL}.}

\item{dir}{A \code{character} directory path to copy files into. The directory
is created if it does not exist.}
}
\value{
A \code{character} vector of full paths to the renamed files, in the
same order as \code{upload}. Returns \code{character(0)} if \code{upload} is \code{NULL}
or empty.
}
\description{
\code{shiny::fileInput()} writes uploaded files to randomly-named tempfiles
(\verb{/tmp/.../0.mzML}) and reports the original names in a separate \code{name}
column. The \code{lcmsPlot()} constructor derives sample IDs from file basenames,
so we copy each upload to a session-scoped directory under its original
name to keep sample IDs meaningful.
}
\keyword{internal}
