% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lcmsPlot-class.R
\name{lp_purity_timeline}
\alias{lp_purity_timeline}
\title{Plot precursor ion purity scores as a timeline}
\usage{
lp_purity_timeline(sample_ids = NULL, threshold = NULL)
}
\arguments{
\item{sample_ids}{A \code{character} vector of sample IDs to include.
\code{NULL} uses all samples.}

\item{threshold}{A \code{numeric} value in \verb{[0, 1]} drawn as a horizontal
reference line. \code{NULL} suppresses the line.}
}
\value{
A layer function for use with the \code{+} operator.
}
\description{
\code{lp_purity_timeline()} generates a scatter plot of \code{inPurity} (y-axis)
versus retention time (x-axis), one point per MS/MS acquisition event,
coloured by sample. An optional horizontal dashed line marks a purity
threshold. Requires the data object to be a \code{purityA} result.
}
\examples{
\dontshow{if (requireNamespace("msPurity", quietly = TRUE)) withAutoprint(\{ # examplesIf}
mzml_dir <- file.path(tempdir(), "purity-example")
utils::unzip(
    system.file("extdata", "standards-mzml.zip", package = "lcmsPlot"),
    exdir = mzml_dir)

ms2_files <- file.path(
    mzml_dir, "mzml",
    c("l-proline-MS2.mzML", "l-kynurenine-MS2.mzML"))

pa <- msPurity::purityA(ms2_files)

## One point per MS/MS event, coloured by sample.
p <- lcmsPlot(pa) +
    lp_purity_timeline(threshold = 0.7)
p
\dontshow{\}) # examplesIf}
}
