% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data-source-compound-discoverer-node.R
\name{.cd_node_write_plot_column}
\alias{.cd_node_write_plot_column}
\title{Write a Compound Discoverer \code{node_response.json} adding a per-compound column}
\usage{
.cd_node_write_plot_column(
  node_args,
  id_to_value,
  column_name = "Plot",
  renderer = "EB29D794-4F2E-4785-8B80-A24D8C0FB3E4",
  position_after = "Name"
)
}
\arguments{
\item{node_args}{A \code{character} path to the \code{node_args.json} file.}

\item{id_to_value}{A \code{data.frame} with columns \code{compound_id} and \code{value}
mapping each Compounds ID to its cell value (e.g. a plot file path).}

\item{column_name}{The new column's header. Default \code{"Plot"}.}

\item{renderer}{The \code{SpecialCellRenderer} GUID string.}

\item{position_after}{An existing Compounds column to place the new column
after. Default \code{"Name"}.}
}
\value{
The path to the written \code{node_response.json} (invisibly), or \code{NULL} if
the \code{node_args} has no \code{ExpectedResponsePath} (e.g. a standalone run).
}
\description{
Writes a \code{node_response.json} that returns \strong{only} the Compounds table (the
one table modified) with one new column appended, matched on the Compounds
table's \code{"Compounds ID"} primary key. Each row's value comes from
\code{id_to_value} (rows without a mapping get an empty string). The column is
displayed in Compound Discoverer via the supplied \code{SpecialCellRenderer} GUID -
e.g. CD's filename renderer, which turns the cell into a clickable file link.
Only the modified table is returned.
}
\keyword{internal}
