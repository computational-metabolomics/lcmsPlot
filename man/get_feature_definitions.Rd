% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data-adapters.R
\name{get_feature_definitions}
\alias{get_feature_definitions}
\alias{get_feature_definitions.default}
\alias{get_feature_definitions.XCMSnExp}
\alias{get_feature_definitions.XcmsExperiment}
\alias{get_feature_definitions.MsExperiment}
\alias{get_feature_definitions.XChromatograms}
\title{Get the stored feature definitions from the data object}
\usage{
get_feature_definitions(obj)

\method{get_feature_definitions}{default}(obj)

\method{get_feature_definitions}{XCMSnExp}(obj)

\method{get_feature_definitions}{XcmsExperiment}(obj)

\method{get_feature_definitions}{MsExperiment}(obj)

\method{get_feature_definitions}{XChromatograms}(obj)
}
\arguments{
\item{obj}{A data object containing or representing samples.}
}
\value{
A \code{tibble} of feature definitions with at least \code{rtmin} and \code{rtmax},
plus \code{mzmin}/\code{mzmax} and \code{row} where the object provides them; or \code{NULL} when
no correspondence results are stored.
}
\description{
\code{get_feature_definitions()} retrieves the correspondence results already
stored on an object, as opposed to \code{get_grouped_peaks()} which reshapes them
for feature labelling. Used by \code{lp_peak_density()} when \code{simulate = FALSE} to
draw the feature groups the object actually holds rather than re-deriving
candidates from the density curve.
}
\keyword{internal}
