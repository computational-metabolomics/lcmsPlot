% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/creators-total-ion-current.R
\name{.tic_from_raw_files}
\alias{.tic_from_raw_files}
\title{Build the total ion current dataset from raw MS files}
\usage{
.tic_from_raw_files(metadata, options)
}
\arguments{
\item{metadata}{A \code{data.frame} of sample metadata with at least the columns
\code{sample_id}, \code{sample_index}, and \code{sample_path}.}

\item{options}{A \code{list} of plot options. \code{options$total_ion_current$sample_ids}
selects which samples to read; \code{options$parallel_param}, when set, is used to
read files in parallel.}
}
\value{
A \code{tibble} with columns \code{intensity}, \code{metadata_index}, and
\code{feature_metadata_id} (one row per MS1 scan).
}
\description{
Reads the per-scan total ion current (TIC) from the headers of the raw
files referenced in \code{metadata} and returns it in the layout expected by the
\code{total_ion_current} slot of an \code{lcmsPlotDataContainer}.
}
\keyword{internal}
