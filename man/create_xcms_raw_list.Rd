% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xcms-raw-list.R
\name{create_xcms_raw_list}
\alias{create_xcms_raw_list}
\title{Create an \code{XcmsRawList} from raw LC-MS files}
\usage{
create_xcms_raw_list(
  paths,
  profstep = 1,
  mslevel = NULL,
  scanrange = NULL,
  BPPARAM = NULL
)
}
\arguments{
\item{paths}{A \code{character} vector of file paths to raw MS files
(e.g. \code{.mzML}, \code{.mzXML}, \code{.CDF}).}

\item{profstep}{A \code{numeric} value passed to \code{xcms::xcmsRaw()} controlling
the mass bin size used to build the profile matrix. Defaults to \code{0}, which
skips profile matrix generation. Only set this to a positive value if you
need the profile matrix for peak detection.}

\item{mslevel}{A \code{numeric} value passed to \code{xcms::xcmsRaw()} indicating
which MS level to load. \code{NULL} loads all levels. Defaults to \code{NULL}.}

\item{scanrange}{A length-2 \code{integer} vector passed to \code{xcms::xcmsRaw()}
restricting the range of scans to read. \code{NULL} reads all scans.
Defaults to \code{NULL}.}

\item{BPPARAM}{A \code{BiocParallelParam} object controlling parallel execution.
When \code{NULL} (default) files are read sequentially.}
}
\value{
An \code{XcmsRawList} object with one \code{xcmsRaw} element per file.
}
\description{
Reads one or more raw MS files using \code{xcms::xcmsRaw()} and wraps the
results in an \code{XcmsRawList}. Optionally runs the reads in parallel via
\code{BiocParallel}.
}
\examples{
paths <- dir(
    system.file("cdf", package = "faahKO"),
    full.names = TRUE,
    recursive = TRUE
)[1:3]
xl <- create_xcms_raw_list(paths)
}
