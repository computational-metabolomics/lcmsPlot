% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-chrom-peak-rects.R
\name{chrom_peak_rects_layer}
\alias{chrom_peak_rects_layer}
\title{Overlay chromatographic peak boundaries on an rt / m/z panel}
\usage{
chrom_peak_rects_layer(
  p,
  detected_peaks,
  options,
  x_dim = "rt",
  rt_range = NULL,
  mz_range = NULL
)
}
\arguments{
\item{p}{A \code{ggplot} object whose axes are retention time and m/z.}

\item{detected_peaks}{A \code{data.frame} of detected peaks, requiring columns
\code{rtmin}, \code{rtmax}, \code{mzmin}, \code{mzmax}, and \code{sample_id}.}

\item{options}{A \code{list} of plot options (reads \code{options$chrom_peak_rects}).}

\item{x_dim}{A \code{character} naming the dimension on the x axis, \code{"rt"}
(default) or \code{"mz"}. The other axis carries the remaining dimension.}

\item{rt_range}{An optional length-2 \code{numeric} clamping the rectangles to the
host panel's retention-time window.}

\item{mz_range}{An optional length-2 \code{numeric} clamping the rectangles to the
host panel's m/z window.}
}
\value{
The modified \code{ggplot} object.
}
\description{
Draws one rectangle per detected chromatographic peak, spanning
\code{rtmin}-\code{rtmax} by \code{mzmin}-\code{mzmax}. This is the primitive behind
\code{xcms::plotChromPeaks()} and \code{xcms::plot(type = "XIC")}: the former draws the
rectangles on an empty frame, the latter over the ion map.
}
\details{
The host renderer passes its own axis assignment, so the overlay follows when
the map is flipped to m/z on x.
}
\keyword{internal}
