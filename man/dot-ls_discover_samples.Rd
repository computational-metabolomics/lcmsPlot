% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data-source-lipid-search.R
\name{.ls_discover_samples}
\alias{.ls_discover_samples}
\title{Discover the 5.2 sample declarations from the table columns}
\usage{
.ls_discover_samples(cols)
}
\arguments{
\item{cols}{A \code{character} vector of the table's column names.}
}
\value{
A \code{tibble} of \code{sample_key}, \code{sample_group}, \code{injection_key} and
\code{file_name} (always \code{NA}), or \code{NULL} when no group columns are found.
}
\description{
LipidSearch 5.2 files carry no \verb{#[key]:file} declarations. The sample keys are
taken from the group-level \code{OrgMeanArea[...]} columns (\code{s1}, \code{s2}, ...); each
key's per-injection data lives in columns suffixed \verb{<key>-<n>} (e.g.
\code{Area[s1-1]}), so the first such injection key is resolved per sample.
}
\keyword{internal}
